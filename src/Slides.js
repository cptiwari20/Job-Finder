import React, { Component } from 'react';
import {View, Text, ScrollView} from 'react-native'

class Slides extends Component {
  render() {
    return (
      <ScrollView>
        <Text>Welcome to the Job Finder</Text>
      </ScrollView>
    )
  }
}

export default Slides;